## 2026-05-02 — Issue #17 selection start versus cancel

- Picked up issue #17 and branched `agent/17-selection-result` from `origin/main` to avoid unrelated local launcher changes.
- Added an explicit selection result state in `internal/ui.SelectionModel` so Enter records a started selection while `q` and Ctrl-C record cancellation.
- Added tests for distinct Enter, `q`, and Ctrl-C outcomes while preserving `Done()` as the general exited flag.
- Updated `docs/codebase/ui.md` for the new selection result helpers.
- Opened PR #26.
- Verification: `go test ./internal/ui`, `go vet ./...`, `staticcheck ./...`, `go test ./...`, `bash scripts/docs-gate.sh`, and `git diff --check` passed. `make ci` could not run locally because this runner's `docker` command does not support `docker compose -f ...`.

## 2026-05-02 — Issue #13 long log lines

- Picked up issue #13 and branched `agent/13-long-log-lines` from `origin/main` to avoid unrelated local launcher changes.
- Added regression coverage for a stream event containing a single log line above `bufio.Scanner`'s default 64 KiB token limit.
- Configured `internal/stream.Manager` with an explicit 1 MiB maximum log-line size so large structured payloads keep streaming without unbounded allocation.
- Updated `docs/codebase/stream.md` to document the bound and the behavior for lines above it.
- Opened PR #25.
- Verification: `go test ./internal/stream`, `go vet ./...`, `staticcheck ./...`, and `go test ./...` passed. `make ci` could not run locally because this runner's `docker` command does not support `docker compose -f ...`.

## 2026-05-02 — Issue #16 selected containers to live log view

- Picked up issue #16 and branched `agent/16-wire-selected-containers-live-log-view` from `origin/main` to keep unrelated local launcher changes out of the PR.
- Added command orchestration that runs selection, exits cleanly when no containers are selected, creates Docker-backed stream sources for selected containers, starts the stream manager, and launches the log view.
- Added command-level tests for zero-selection handling and selected-container stream wiring into the log view.
- Opened PR #20.
- Verification: `go test ./cmd/docker-log`, `go test ./...`, `go vet ./...`, and `staticcheck ./...` passed. `make ci` could not run locally because this runner's `docker` command does not support `docker compose -f ...`.

## 2026-05-02 — Issue #14 Docker log stream opener

- Picked up issue #14 and branched `agent/14-add-docker-log-stream-opener` from `origin/main` to avoid unrelated local launcher changes.
- Added `Client.OpenContainerLogs(ctx, container)` in `internal/docker`, using Docker SDK `ContainerLogs` with follow enabled, stdout/stderr included, and `Tail: "0"` for live-only streams.
- Added fake API tests for log option wiring, returned reader propagation, and wrapped Docker errors.
- Updated `docs/codebase/docker.md` for the new adapter API.
- Opened PR #19.
- Verification: `go test ./internal/docker`, `go test ./...`, `go vet ./...`, `staticcheck ./...`, and `bash scripts/docs-gate.sh` passed. `make ci` could not run locally because this runner's `docker` command does not support `docker compose -f ...`.

## 2026-05-02 — Issue #3 multi-container selection UI

- Picked up issue #3 and branched `agent/3-build-multi-container-selection-ui` from updated `main` after the bootstrap PR landed.
- Added `internal/ui.SelectionModel`, a Bubble Tea model for container list rendering, keyboard navigation, multi-select toggling, empty state output, and clean quit/start actions.
- Added tests for multi-select ordering, cursor wrapping, empty state rendering, and quit commands.
- Added `docs/codebase/ui.md` for the new UI module and recorded user-facing progress in `logs/progress.md`.
- Verification: `go test ./...`, `go vet ./...`, and `git diff --check` passed with a repo-local Go toolchain. `make ci` was blocked locally because Docker is not installed; GitHub CI passed.

## 2026-05-02 — Issue #2 Docker container discovery

- Added `internal/docker` as the single Docker SDK adapter for running-container discovery.
- Normalized container ID, name, image, and status into `domain.Container`.
- Wired CLI startup to list containers and open the existing selection UI.
- Added unit tests for adapter success/failure paths and status rendering.
- Verification: `go test ./...`, `go vet ./...`, and `staticcheck ./...` passed with a repo-local Go 1.22.12 toolchain; `make ci` could not run because this runner has no Docker binary.
