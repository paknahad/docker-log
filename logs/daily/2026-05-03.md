## 2026-05-03 — Filter mode state

- Picked ready-for-agent issue #40 (`priority:med`) and opened PR #41 on `agent/40-filter-mode-state`.
- Kept scope to internal filter state and matching behavior; UI controls for toggling regex or case sensitivity remain out of scope.
- Added `filter.State` with text, regex-enabled, and case-sensitive fields.
- Preserved default plain-text, case-sensitive filtering through `NewState` and existing `NewMatcher`/`Lines` helpers.
- Added regex and case-insensitive matching support in `internal/filter`, with invalid regex patterns returned as validation errors.
- Updated the log view to store filter state while keeping current text-entry behavior unchanged.
- Updated `docs/codebase/filter.md` and `docs/codebase/ui.md` for the new filter-state contract.
- Local direct checks passed: `go test ./internal/filter`, `go test ./internal/ui`, `go test ./...`, `go vet ./...`, and `staticcheck ./...`.
- Local `make ci` could not run because the host Docker command does not support `docker compose`.
- GitHub CI passed on PR #41, and the PR was squash-merged.

## 2026-05-03 — Bounded stream buffering

- Picked ready-for-agent issue #6 (`priority:med`) and opened PR #31 on `agent/6-bounded-buffering`.
- Added an explicit 4,096-event cap to the stream manager channel buffer.
- Documented the blocking backpressure strategy in `internal/stream/manager.go` and `docs/codebase/stream.md`.
- Added stream coverage for buffer capping and backpressure when the event channel is full, plus a bounded fan-in benchmark.
- Local direct checks passed: `go test ./internal/stream`, `go test ./...`, `go vet ./...`, `staticcheck ./...`.
- Local `make ci` could not run because the host Docker command does not support `docker compose`.

## 2026-05-03 — Container lifecycle disconnect visibility

- Picked ready-for-agent issue #7 (`priority:med`) and opened PR #32 on `agent/7-container-lifecycle`.
- Scoped the broad lifecycle request to clean stream disconnect visibility, leaving Docker event watching and reconnect loops out of this cycle.
- Added `stream.Event.Disconnected`, emitted it when a container stream reaches EOF without context cancellation, and rendered it as a buffered log-view status line.
- Added stream, UI, and runtime tests for disconnect behavior while keeping unrelated streams active.
- Updated `docs/codebase/stream.md` and `docs/codebase/ui.md` for the new lifecycle event.
- Local direct checks passed: `go test ./...`, `go vet ./...`, `staticcheck ./...`, and docs-gate with `DOCS_GATE_SOURCE_ROOT=internal`.
- Local `make ci` could not run because the host Docker command does not support `docker compose`.

## 2026-05-03 — Colorized log prefixes

- Picked ready-for-agent issue #29 (`priority:low`) and opened PR #34 on `agent/29-colorize-container-names`.
- Kept the work scoped to `internal/ui`: container prefixes are colorized at render time while message content remains plain.
- Stored plain and display forms of buffered log lines so filtering does not match ANSI escape sequences.
- Added fallback behavior for `NO_COLOR`, `CLICOLOR=0`, and `TERM=dumb`.
- Added UI tests for prefix-only coloring, stable per-session color assignment, plain fallback rendering, and filtering colorized lines by plain text.
- Updated `docs/codebase/ui.md` for the new log-view rendering behavior.
- Local direct checks passed: `go test ./internal/ui`, `go test ./...`, `go vet ./...`, `staticcheck ./...`, and docs-gate with `DOCS_GATE_SOURCE_ROOT=internal`.
- Local `make ci` could not run because the host Docker command does not support `docker compose`.

## 2026-05-03 — Ctrl+C-only application exit

- Picked ready-for-agent issue #33 (`priority:med`) and opened PR #35 on `agent/33-remove-q-quit-shortcut`.
- Removed `q` from selection cancellation and log-view quit handling so it no longer exits the application.
- Preserved Ctrl+C as the clean exit/cancel key in both UI models and updated selection help text.
- Added regression tests that `q` does not quit selection, `q` becomes filter text in the log view, and Ctrl+C still exits.
- Updated `docs/codebase/ui.md` for the revised UI key behavior.
- Local direct checks passed: `go test ./internal/ui ./cmd/docker-log`, `go test ./...`, `go vet ./...`, `staticcheck ./...`, and docs-gate with `DOCS_GATE_SOURCE_ROOT=internal`.
- Local `make ci` could not run because the host Docker command does not support `docker compose`.

## 2026-05-03 — README core workflow refresh

- Picked ready-for-agent issue #23 (`priority:low`) and opened PR #36 on `agent/23-refresh-readme-core-workflow`.
- Kept scope to `README.md`, updating the project description and quick start to match the shipped local Docker workflow.
- Preserved the local-only positioning and avoided hosted storage, Kubernetes, cloud, release packaging, or broader documentation changes.
- Local `make ci` could not run because the host Docker command does not support `docker compose`.
- Local direct checks passed: `go test ./...`, `go vet ./...`, and `staticcheck ./...`.

## 2026-05-03 — Message-only log filtering

- Picked ready-for-agent issue #37 (`priority:med`) and opened PR #39 on `agent/37-filter-message-content`.
- Scoped the change to the log-view buffer so rendered container prefixes remain visible but no longer participate in filtering.
- Added UI regression tests for log-content matches, container-name-only non-matches, and multiplexed container streams.
- Updated `docs/codebase/ui.md` for the separate display text and filter target stored by the log view.
- Local direct checks passed: `go test ./internal/ui`, `go test ./...`, `go vet ./...`, `staticcheck ./...`, and docs-gate with `DOCS_GATE_SOURCE_ROOT=internal DOCS_GATE_EXT=go`.
- Local `make ci` could not run because the host Docker command does not support `docker compose`.
- GitHub CI passed on PR #39.

## 2026-05-03T01:36:27Z — Issue #42

- Added regex-mode support to the log view via the existing filter state.
- Kept regex matching scoped to stream message content, not rendered container prefixes.
- Exposed invalid regex state through `LogModel.FilterError()` and rendered feedback in the log view.
- Verified with `go vet ./...`, `staticcheck ./...`, and `go test ./...`; `make ci` could not run because this Docker CLI lacks `docker compose`.
